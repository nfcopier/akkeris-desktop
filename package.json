{
  "name": "akkeris-desktop",
  "version": "1.0.0",
  "devDependencies": {
    "@types/react": "^17.0.36",
    "@types/react-dom": "^17.0.11",
    "copyfiles": "^2.4.1",
    "electron": "^15.3.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "rxjs": "^7.5.5"
  },
  "main": "./build/app-start.js",
  "scripts": {
    "start": "npm run build && electron .",
    "clean": "rimraf build/",
    "copyStaticAssets": "copyfiles -u 1 client/static/**/* build",
    "build": "npm run clean && tsc && npm run copyStaticAssets"
  }
}
